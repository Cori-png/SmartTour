{
  "name": "smarttour-root",
  "private": true,
  "version": "1.0.0",
  "description": "SmartTour Bénin - Racine du projet",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "dependencies": {
    "convex": "^1.39.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
